# Leave empty to use Vite's dev-server proxy (default for local development).
# Set to the backend URL when running frontend separately from the backend.
# Example: VITE_API_URL=http://localhost:3001
VITE_API_URL=
